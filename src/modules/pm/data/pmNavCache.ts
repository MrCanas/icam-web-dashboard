/**
 * Etiqueta de caché de la lista de proyectos de la nav PM
 * (fetchPmProjectNavItems). Quien cree, archive, restaure o vincule proyectos
 * la invalida con updateTag para que la nav no espere a que caduque.
 */
export const PM_NAV_CACHE_TAG = "pm-nav-projects";
