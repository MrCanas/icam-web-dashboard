/**
 * Movido a `src/components/ui/ToolbarPopover.tsx`: lo usan ahora las barras
 * flotantes de portfolio y de corporativo, así que dejó de ser de un módulo.
 * Este re-export mantiene válidos los imports existentes.
 */
export { ToolbarPopover } from "@/components/ui/ToolbarPopover";
export type { ToolbarPopoverProps } from "@/components/ui/ToolbarPopover";
