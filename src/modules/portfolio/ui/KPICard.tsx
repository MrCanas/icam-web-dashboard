/**
 * Movida a `src/components/ui/KPICard.tsx`: es la tarjeta de KPI del portal
 * entero (portfolio, pm y corporativo), no de un módulo. Deuda anotada en el
 * README del módulo y ya saldada; este re-export mantiene válidos los imports.
 */
export { KPICard } from "@/components/ui/KPICard";
